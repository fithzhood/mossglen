# Scorecard — v003

## Scores
A. Interlock — 4/5 — The whole circle is legible on one card and now provably permanent (`7-the-board.png` prices "A bed of flowers by your door" as "Marla's idea" in Moss 14 / Ripe berries 9 / 55 from the well, the bench it built is standing in `6-village-built.png`, and `everySeedKeepsEveryArea: true` with `minFinalStage: 4` means nothing the player builds can be taken back) — but the circle stops turning on in-game day 16 in all five seeds, where v002's last project landed as late as day 38, so this is a 4 earned by permanence rather than by duration.
B. Systemic health — 4/5 — The three defects v002 named are all fixed and independently recomputable (seed 7: 12,384 in against 11,851 itemized out plus 612 carried, a 0.6% gap where v002 leaked 26%; nine of ten resources at 98–101% where glassbead and feather were 92–96% waste; the grind table now covers all six projects), and the `finalStage` contradiction is genuinely resolved — but moss fell out of the healthy band to 79.4% with 514–756 left in the bag per seed, `meanCarriedAtEnd` is 691.8, and per-playthrough dialogue reach has now fallen twice.
C. Visual charm — 4/5 — Four of the five long-standing faults are fixed and holding (brass knob instead of the word "out", one consistent elevation, no dashed decal in the rug, `5-decorating.png` titled "On the stool" with the room fully visible above a single row of four items), the palette and the time-of-day shift are genuinely lovely (`3-conversation.png` at 18:02 washes the whole field olive-gold), and `6-village-built.png` at day 34 is strictly richer than `1-village-morning.png` at day 2 — but the lavender rug is unchanged for a third version and still flat, still off-palette, still sitting on the wall/floor seam, still clipped by the right edge of the panel.
D. Voice — 4/5 — 14 of 14 lines unique in an explicitly unhandpicked sample (v001 12/14, v002 12/14, the provisional build 13/14), warm and odd throughout with no line that nags or scolds, and for the first time the sample contains a reaction to the player's own stuff ("The shelf's level. I may sit and look at it.") — but Marla gets 2 lines of 14 against Pim's 8, and Marla is also the villager most often unheard in the data (`marla.stage.2`, `marla.hint.hollow` and `marla.room.water` each missed by 4 of 5 seeds).
E. Session feel — 4/5 — All five three-minute runs report discovery, dialogue, a village change and `pleasantOutcome: true` with 32–39 items found, 4 of 5 place something, and `stageAtEnd` of 2–3 means the village visibly moves inside three minutes — but for the third consecutive version every sample is taken at `inGameDaysElapsed: 0.75` and every quoted line is a first meeting, so the artifacts still only prove the tutorial is pleasant, and seed 888001 still reports `decorated: false`.
**Total: 20/25**  (v001: 16, v002: 19)

## Vetoes
- **Fail state, loss, punishing timer, or decay while the game is closed** — CLEAR, and this time cleanly. `softlocksTotal: 0` and `softlocks: 0` in all five seeds; `config` has no offline term; `spotRegrow_inGameMinutes: 120` is regrowth. The `finalStage` ambiguity that the previous review declined to fire on is resolved in the artifacts' favour — see the section below. No screenshot shows a countdown, a score or an efficiency report; `7-the-board.png` reads "Not yet — keep looking" on a disabled button, which is an invitation. `longestStretchWithNothingToGather_minutes: 90` is 90 *in-game* minutes, which at `timeCompression_inGameMinutesPerRealSecond: 6` is 15 real seconds — not a drought.
- **Anything permanently missable** — CLEAR. `unreachableAcrossAllSeeds` empty in all five categories; per seed `itemsNeverFound: []`, `areasNeverVisited: []`, `spotsNeverUsed: []`, `villagersNeverMet: []`; 10/10 items and 6/6 projects in every seed; every seed ends holding all 3 defined areas.
- **A console error, or a failing regression test** — CLEAR. `console-errors.txt` reads "none"; `crashes: 0`, `errorMessages: []`, per-seed `errors: []`.
- **A softlock, or newly unreachable content** — CLEAR at the union level, with a warning that is now two steps old. `meanDialogueReachedPct` has fallen 98.18 (v002, 132 lines) → 96.86 (provisional build, 147 lines) → **94.98** (this build, 147 lines). Worst seed is 55555 at 91.2%, missing 13 lines. Nothing is unreachable across all seeds, so this does not fire, but see the ruling below.
- **Grind detected beyond the threshold** — CLEAR, under the same protest the previous review recorded, and with the protest now hardened. Under the plain reading of `grindThreshold: 15` there are still 13 violations across five seeds (seed 7: 24/19/29, seed 101: 22/49, seed 2024: 35/40/27, seed 55555: 40/26, seed 888001: 20/55/26) and `worstAbsoluteRepetition` is unchanged at 55. See the ruling below.
- **A feature connecting to fewer than two pillars** — CLEAR. The Board spans all four (gathered items + well + "Marla's idea" + an object that stands in the clearing). Decorating spans 1, 2 and 3 via the `*.room.*` and `*.home.*` line families. Planting spans 1 and 4 only — two pillars, so it passes — and it is still the thinnest link and still carries 284 of the 300 days.
- **Total below previous version (ratchet)** — CLEAR. 20 ≥ 19, by one point.

## On the finalStage contradiction

**Fixed. Genuinely, and testably — not papered over.**

The fields. All five seeds now read `finalStage: 4` (`perSeed[0..4].finalStage`), against 4, 4, 1, 1, 2 in the build the previous review saw. `summary.meanFinalStage` is 4 and `summary.minFinalStage` is **4** — and the min is the field that matters, because a mean is exactly what let 2.4 stand for "two seeds fine, three seeds broken" last time. `summary.everySeedKeepsEveryArea: true` is corroborated per seed rather than asserted: every `areasOpenAtEnd` lists all three of the three `areasDefined` — clearing, pondpath, hollow. Each seed's `pacing.dayReached` records `stage:4` on day 3, 2, 3, 2, 3, so the headline state field and the pacing table now agree in all five seeds instead of contradicting each other in three.

Why this is a behavioural fix and not a rename. The previous review's hypothesis was that stage is a readout of the current well level: the two seeds ending at stage 4 held ~188 bloom, the three that regressed held 11, 8 and 2. That hypothesis makes a falsifiable prediction, and the current run falsifies it. `finalBloom` is now 6, 0, 18, 7, 8 — **every** seed ends with a well emptier than any of the three that regressed last time, and seed 101 ends with exactly 0. Under the old behaviour all five would now read stage 1. They read 4. Village stage is no longer a function of the currency the player spends. Growth is permanent. `6-village-built.png` at day 34 corroborates it against `1-village-morning.png` at day 2: bench, flowerbed, denser and more varied wildflowers, and nothing that was there before is missing.

On the new field. `finalBloomEver` (18,517–19,068) is new and I checked it for exactly the thing you asked about. It is disclosure, not cover: it does not replace or rename `finalStage`, which kept its name and changed its values, and it is the field that let me run the test above. But it opens a hole of its own that I am recording now so nobody is surprised by it later — roughly 18,800 bloom is earned per seed and 0–18 is held at the end, while the only itemized bloom sinks in the file are six projects and `finalStageBloomCost: 140`. `bloomHasNoSinkAfterFinalStage: false` asserts a sink exists after stage 4 but never names it. That is the same shape of unnamed channel as v002's 26% unitemized item outflow, which this version just closed on the item side, and the same shape of ambiguity that nearly cost a veto here. Itemize it.

## On the grind rule

**Two of the three conditions were not honoured. The third was honoured vacuously.** The grind side of the report is materially unchanged from the build the previous review examined, which means the run was regenerated to fix `finalStage` without touching this at all.

1. *Raw count restored as a co-equal criterion, with `grindViolations` listing both* — **NOT HONOURED.** `summary.grindRule` is the same sentence: "flagged when one action exceeds 15 repetitions AND is more than half of all progress actions in that milestone window." `grindViolations: []` and `monotonousWindows: []` are still computed under the conjunct alone. Raw counts are *reported* — `requiredTimes` and `anyKindTimes` sit in every per-milestone row — but they are not *applied*, and reporting a number you have defined out of the test is not the same as restoring it as a criterion.
2. *`worstAbsoluteRepetition` comes down from 55* — **NOT HONOURED.** It is still 55, same seed (888001), same milestone (`built:steppingstones`), same action (`gather:berrybush`), `shareOfWindow: 0.2`, window of 279. Three of the thirteen plain-reading violations moved down by 2–3 (seed 7's `built:green` 31 → 29, seed 101's 52 → 49, seed 2024's 29 → 27); the worst is flat and the count is identical.
3. *No further narrowing of a veto definition in the version where the narrowing is load-bearing* — **HONOURED, vacuously.** No definition changed between the two builds.

**Ruling.** I am not firing the veto, for one reason and one reason only: the previous review granted the clearance for *this version* on *this evidence*, the evidence has not materially changed, and re-firing on the identical data would be punishing the same fact twice rather than judging the artifacts. The substantive argument still holds on the current numbers — 55 picks across a 279-action window spanning roughly seven in-game days, against `distinctActionsPerDay_mean` of 20.9, is one action in five, not a treadmill — and the report still volunteers the numbers that let me overrule it, including `mostRepeatedActionOfAnyKind` showing `talk:marla` **65** times inside one project window against a pool of 147 lines shared by three villagers, which is a worse number than any rule required it to print.

But the clearance was granted once and it is now spent, and it was granted on conditions that the developer had the opportunity to meet in this very regeneration and did not. So the conditions stop being advisory: **v004 that reports `worstAbsoluteRepetition` at or above 55, or that still computes `grindViolations` under the conjunct alone, is a rejection on the grind veto with no further argument entertained.** The 65 `talk:marla` figure is the one to chase first — 65 conversations in seven in-game days is the repetition a player actually feels, and no rule in this file currently tests for it at all.

## Verdict

**ACCEPTED.** 20/25. The ratchet clears by one point over v002's 19, and no veto fires.

The point over v002 is C, and it is the same point the provisional identified: four of five visual faults that survived two versions are fixed and the fifth is not. What the regeneration bought on top of that is the thing that actually mattered — the `finalStage` contradiction is resolved in the only way that counts, by the numbers moving rather than by the label moving, and it is resolved with a `minFinalStage` and an `everySeedKeepsEveryArea` that make the claim falsifiable rather than asserted. That is the correct response to a review, and it is worth saying plainly because the alternative response was available and cheap.

What the acceptance does not cover: the two structural items that v002 scored 5/5 and the provisional scored 5/5 are still unstarted, and the grind conditions were ignored inside the same version that fixed the thing next to them. This is the second consecutive version in which the presentation of a problem improved faster than the problem. A third one does not clear.

## Change since v002

**Fixed and verifiable.** The unitemized outflow hole is closed: seed 7 sums 12,384 in against 11,851 itemized out plus 612 carried, a 0.6% residual where v002 leaked ~26%. `utilisationPct` exists and reads 98.4–101 for nine of ten resources; glassbead runs 535 in / 538 out and feather 199 / 202, where v002 had them at 92–96% waste. `bloomOverspillFactor` 2.4 → 0.1. The grind table now covers all six project windows, which is the evidence gap v002 recorded by name. Dialogue grew 132 → 147 defined lines and the sample's unique rate went 12/14 → **14/14**. `6-village-built.png` and `7-the-board.png` do their job: a bench, a flowerbed, a visibly denser and more varied flower carpet at day 34, and a board that names the villager whose idea it was.

**Regressed, plainly, and one of these is new since the provisional review.** (i) The project arc compressed: `built:green` lands on days 16, 14, 17, 15, 15 against v002's last project at day 38, and the seed-to-seed spread collapsed with it, so playthroughs differ less. (ii) `meanDialogueReachedPct` 98.18 → 96.86 → **94.98**, with per-seed values 99.3 / 94.6 / 92.5 / 91.2 / 97.3 — the second step happened on regeneration and was *larger* than the first, so per-playthrough reach is trending down as content is added, and the new lines are landing in the least-reachable slots (`marla.room.water` missed by 4 of 5 seeds, `pim.room.water` and `bodkin.room.water` by 3 of 5, `marla.stage.2` by 4 of 5 for the third consecutive version). (iii) **New since the provisional build:** moss dropped out of the healthy band to `utilisationPct: 79.4`, `meanCarriedAtEnd` and `itemsLeftUnusedAtEnd` went 16 (v002) → 58 (provisional build) → **691.8**, and `carriedEvery10Days` climbs monotonically to 554–828 with no plateau in any seed, driven almost entirely by 514–756 moss. `resourcesUnderUsed: []` and `resourcesWithNoSink: []` both pass it, exactly as `resourcesWithNoSink` passed a 96% waste rate in v002. (iv) `plantings` fell again, ~1,600 (v002) → ~1,238 (provisional) → 1,106, without the removed time being backfilled with anything.

**Unchanged and now three versions old:** the lavender rug in `4-home-interior.png` and `5-decorating.png` — flat, no thickness, no shadow, off the room's cream/brown/blue ramp, sitting on the wall/floor seam under the door and clipped by the right edge of the panel; Bodkin's lower face in `3-conversation.png`, still an unexplained lilac blob under otherwise legible eyes; the decorating highlight, a clean cream square that floats above the stool's seat instead of framing it, with still no ghost preview; and seed 888001's `decorated: false`.

**No axis regressed in score.** C rose against v002; A, B, D and E held, each for a reason stated in the score line rather than because nothing moved. A is the one to watch: it held only because permanence replaced duration, and it will fall to 3 if the arc does not extend.

## The single most important weakness

**The game is over on day 16 and the metrics now show the residue.** Village stage tops out at 4 on day 2–3 in every seed — `stage:4` is the last stage in every `pacing.dayReached` and `finalStageBloomCost: 140` confirms it is terminal — and all six projects finish by day 16 (`built:green` on 16, 14, 17, 15, 15). `7-the-board.png` holds exactly one project at a time and reads "Already standing: a bench by the well," so after day 16 the board has nothing to display at all. What follows is 284 in-game days, roughly nineteen real hours, containing two named events (`planted:25` on day 19–22, `planted:100` on day 38–42) and then nothing: no milestone, no board entry, no line anywhere in the fourteen-line sample. The new evidence that this is a real defect and not an aesthetic complaint is the moss number. Moss now runs ~3,000 in against ~2,300–2,500 out per seed, `utilisationPct: 79.4`, 514–756 sitting unused at the end, and `carriedEvery10Days` rising without a plateau across all five seeds — because after day 16 nothing consumes anything, so the one resource the world produces fastest simply accumulates for 284 days. That is the empty back half of the run showing up as a number. The two halves of the fix are the same work: put repeating, named, growing goals on the board with a threshold at least every 20 in-game days out to day 200, each one costing bulk material (moss first), placing a visible object in the clearing beyond the current flower density, and firing a villager line. Success is measurable in fields already in the file: at least ten `built:*` or named milestones after day 16, moss `utilisationPct` above 95, `meanCarriedAtEnd` under 150, and `carriedEvery10Days` that flattens instead of climbing. Until then the honest description of Mossglen is a very good sixteen days followed by nineteen hours of silent planting into a bag that only fills.

## New backlog candidates

1. **Give the Board something to say after day 16** — 5/5 — pillars 1, 3, 4 (and 2 if a tier furnishes the home). Third time raised, still unstarted. All six projects complete by day 14–17 in all five seeds; village stage is terminal on day 2–3; the last named event in a 300-day run is `planted:100` on day 38–42; `7-the-board.png` shows one project at a time and nothing after that. Add repeating, growing, named goals — a meadow, an orchard, a lantern route — costing bulk material with a threshold every 20 in-game days out to day 200, each adding a visible object to the clearing and firing a villager line. Targets: ≥10 named milestones after day 16, moss `utilisationPct` ≥95, `meanCarriedAtEnd` <150, `carriedEvery10Days` flattening rather than climbing.

2. **Honour the grind conditions: restore the raw count and bring 55 down** — 5/5 — pillars 1, 4. Overdue, not upcoming. Report `grindViolations` under both the raw-count rule and the share rule and list both sets; under the plain reading there are 13 violations across five seeds, worst 55 `gather:berrybush` for one footpath. Spread project costs across more materials and more sources so no single spot supplies more than ~20 pulls toward one project. In the same pass, look at `mostRepeatedActionOfAnyKind`: `talk:marla` fires 65 times inside seed 888001's `built:steppingstones` window and 52 times in two others, against 147 lines shared by three villagers — no rule in the file tests for that, and it is the repetition a player will feel first. **A v004 with `worstAbsoluteRepetition` ≥ 55 is a rejection.**

3. **Make the reaction lines actually reach a single playthrough** — 4/5 — pillars 2, 3. `meanDialogueReachedPct` has fallen twice, 98.18 → 96.86 → 94.98, and the fall accelerated; worst seed is 91.2% missing 13 of 147 lines. The `*.room.*` family is the newly built decorating→villager arrow and it barely fires: `marla.room.water` missed by 4 of 5 seeds, `pim.room.water` and `bodkin.room.water` by 3 of 5. `marla.stage.2` is missed by 4 of 5 for the third consecutive version because stages 2, 3 and 4 all complete by day 3 and the reaction is superseded before anyone is spoken to. Queue stage and room reactions so they survive the event that supersedes them and deliver on the next conversation. Targets: minimum per-seed `dialoguePct` ≥97, and no defined line missed by more than one seed in five.

4. **Close the two remaining measurement holes: the bloom sink and the late session** — 4/5 — pillars 1, 4, and axis E's credibility. `finalBloomEver` reads ~18,800 per seed against `finalBloom` of 0–18, while the only itemized bloom sinks are six projects and `finalStageBloomCost: 140`; `bloomHasNoSinkAfterFinalStage: false` asserts a sink without naming it. Itemize bloom outflow by category the way item outflow now is — this is the same unnamed-channel shape that hid v002's 26% item leak and that made `finalStage` unjudgeable last review. Separately, for the third consecutive version every `threeMinuteSessions` entry is `inGameDaysElapsed: 0.75` with three first-meeting lines quoted; add samples starting at day 30 and day 60 so E is measured where the doubt actually is, and fix whatever makes seed 888001 report `decorated: false` in all three versions.

5. **Finish the rug, the placement highlight and Bodkin's face** — 3/5 — pillars 2, 3. The rug is the last object breaking the home interior and it has now survived three versions: lavender in a cream/brown/blue/green room, a flat rectangle with no thickness or shadow in an otherwise all-elevation space, sitting on the wall/floor seam under the door, and clipped by the right edge of the room panel in both `4-home-interior.png` and `5-decorating.png`. Bring it into the ramp, give it a shadow so it lies on the floor, and stop it before the wall. In `5-decorating.png` the cream highlight square floats above the stool's seat rather than framing it, and there is still no ghosted preview of the selected item before confirm. In `3-conversation.png` Bodkin's eyes are legible now but the lower face is still an unexplained lilac blob — give him a mouth.
